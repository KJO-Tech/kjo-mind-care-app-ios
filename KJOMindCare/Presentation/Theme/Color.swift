import SwiftUI

public extension Color {
    
    // MARK: - Theme Access
    /// Provides easy access to the AppTheme colors directly from Color
    /// Usage: Color.theme.primary
    static let theme = AppTheme.Colors.self
}
